@model IEnumerable<AttendanceSystem.Models.LeaveCount>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "CreateEmplyeeOverALlLeave")
</p>
<table class="table table-bordered table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EarnLeave)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SickLeave)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CasualLeave)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeId)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EarnLeave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SickLeave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CasualLeave)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.Name)
            </td>
            @*<td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>*@
        </tr>
    }

</table>
