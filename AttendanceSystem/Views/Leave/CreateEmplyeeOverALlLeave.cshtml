@model AttendanceSystem.Models.LeaveCount

@{
    ViewBag.Title = "EnterEmplyeeLeaveCount";
}

<h2>EnterEmplyeeLeaveCount</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>LeaveCount</legend>

        <br/>

        
        <div class="editor-label">
            @Html.LabelFor(model => model.EmployeeId, "Employee")
        </div>
        <div class="editor-field">
            @Html.DropDownList("EmployeeId", String.Empty)
            @Html.ValidationMessageFor(model => model.EmployeeId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EarnLeave)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EarnLeave)
            @Html.ValidationMessageFor(model => model.EarnLeave)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.SickLeave)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SickLeave)
            @Html.ValidationMessageFor(model => model.SickLeave)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.CasualLeave)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.CasualLeave)
            @Html.ValidationMessageFor(model => model.CasualLeave)
        </div>

        

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
